# AGENTS.md

# Python/Flask Processing Backend
use `uv` 

- contains a backend for qr detection and conversion
- contains a separated web-based caliberation tool to update qr detection parameter

# Unity Integration Client
An remote machine with camera hardware is used to run the full unity scripts
Local machine can only provide non-hardware or mocked tests related tests

# Version Control

Any changes made must be grouped and commited. Commit message should follow convention like feat(x): details
